const ButtonForDeleteSchedule = ({onClick}) => {
    return ( <Button variant="contained" color="primary" onClick={
        onClick
    }>
        Delete
    </Button>)
}

export default ButtonForDeleteSchedule